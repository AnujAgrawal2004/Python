limit=int(input("Enter the limit upto which you want prime numbers "))
list=[]
for i in range(2,limit+1):
    for j in range(2,int(i/2)+1):
       if(i%j==0):
           break
    else:
       list.append(i)
print(list)
